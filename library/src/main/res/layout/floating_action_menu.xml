<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:fitsSystemWindows="true">

    <!--Menu button-->
    <!--Place floating button in frame layout so that we can change it's margin-->
    <!--Which make sure button is not shadowed when running scale animation -->
    <FrameLayout
        android:id="@+id/fbMenuBtnLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true">

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/fbMenuBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="12dp"
            android:src="@drawable/ic_circle_menu"
            app:backgroundTint="#28ABE3"
            app:fabSize="normal"/>

    </FrameLayout>

    <!--Holder for placing menu item-->
    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/fbMenuBtnLayout"
        android:layout_alignEnd="@id/fbMenuBtnLayout"
        android:scrollbarThumbVertical="@android:color/transparent">

        <LinearLayout
            android:id="@+id/itemHolder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="20dp"
            android:gravity="end"
            android:orientation="vertical">

        </LinearLayout>

    </ScrollView>

</RelativeLayout>
